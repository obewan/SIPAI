# Require to install pre-commit and run once: pre-commit install --hook-type commit-msg 
repos:
 - repo: https://github.com/compilerla/conventional-pre-commit
   rev: v3.2.0
   hooks:
     - id: conventional-pre-commit
       stages: [commit-msg]
       args: []